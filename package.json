{
  "name": "internal-operations-service-hub",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start:backend": "npm run start:dev --workspace=@internal/backend",
    "build:backend": "npm run build --workspace=@internal/backend",
    "test:backend": "npm run test --workspace=@internal/backend"
  }
}

